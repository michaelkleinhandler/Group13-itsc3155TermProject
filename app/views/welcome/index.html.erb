<header>
</header>

<body>
<div class="container">
  <div class="row" , style="margin-top:2%;">
    <% if not user_signed_in? %>
      <div id="formWrapper" class="column">
        <%= render 'userLogin' %>
      </div>
    <% end %>
    <% if not user_signed_in? %>
    <% else %>
      <div class="column">
      <%= render 'userNav' %>
    </div>
    <% end %>
    
  </div>
</div>
</body>
